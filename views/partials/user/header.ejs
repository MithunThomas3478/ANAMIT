<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ANAMIT  -  Men's Fashion Store</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="home.css">
    
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <!-- Brand Name on Left -->
            <a class="navbar-brand" href="#">ANAMIT</a>
    
            <!-- Links at Center -->
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="#">TOPWEAR</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">BOTTOMWEAR</a>
                    </li>
                </ul>
            </div>
    
            <!-- Search and Icons on Right -->
            <div class="d-flex align-items-center">
                <input type="text" class="form-control search-bar" placeholder="Search">
                <% if (locals.user) { %>
                    <div class="profile-menu">
                        <span class="icon bi bi-person-circle" title="Profile" onclick="toggleDropdown()" style="font-size: 30px; cursor: pointer;">
                        </span>
                        <div class="dropdown-menu" id="dropdown">
                            <a href="#" class="username"><%= user.name %></a>
                            <a href="/profile">Profile</a>
                            <a href="/logout">Logout</a>
                        </div>
                    </div><span class="icon bi bi-heart" title="Wishlist"></span>
                    <span class="icon bi bi-cart-fill" title="Cart"><sup></sup></span>
                </div>
                <% } else { %>
                    <!-- Login/Signup -->
                    <a href="/login" class="auth-links">LOG IN</a>
                    <a href="signup" class="auth-links">SIGN UP</a>
                   
                <% } %>
        </div>
    </nav>
    
